HttpServerConfig {
  port = 8080
  port = ${?PORT}

  host = "127.0.0.1"
  host = ${?HOST}

  nThreads = 0
  nThreads = ${?NTHREADS}
}

PgDataSource {
  dataSourceClassName = org.postgresql.ds.PGSimpleDataSource
  dataSource {
    #url = "jdbc:postgresql://10.5.0.3:5432/libra"
    url = "jdbc:postgresql://localhost:5432/libra"
    user = postgres
    password = "12345"
  }
}
